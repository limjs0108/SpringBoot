모델 안에 req 있음
model.addAttribute("name",name);
화면쪽에서 name 사용 가능함.

<h1 th:text="|${name} 님 지각입니다.|"><h1>

int 초기값 0 실수 초기값 0.0 문자열 null 

확률
	단위 10 % -> 10칸짜리 배열 생성
	60% 일 때는 6번 반복 돌리면서 배열에 1 6개 들어감
	random.nextInt(10) 

Random r = new Random();
int idx = r.nextInt(10); (0~9 까지 랜덤)
if(ar[index] == 1){
	

	var 

	               ----let	변수 재할당 가능
변수 재선언 불가-----|
	               ----const 변수 재할당 불가능